package classe;

public class VariaveisLocais {
    public static void main(String[] args) {
        // Crio uma variável local e a inicializo.
        int a = 1;

        // Crio um objeto que não referencia nenhum espaço na memória
        String testeNulo = null;

    }
}
/*
-> Valores padrão:
        Inteiro: 0
        Com ponto flutuante: 0.0
        Verdadeiro/falso: false
        Caractere: \u0000

-> Variáveis locais são variáveis que foram criadas dentro do escopo de um método e só estarão disponíveis dentro desse método.
-> Variáveis locais devem ser inicializadas antes de serem usadas.
-> Null significa que a variável não aponta para nenhum espaço na memória.*/
